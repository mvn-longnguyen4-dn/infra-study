on:
  schedule:
    - cron: "0/10 * * * *"
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        shell: bash
        run: |
         echo "TODAY=$(date + %y%m%d) >> $GITHUB_ENV
         echo Hello world, to day is $TODAY
